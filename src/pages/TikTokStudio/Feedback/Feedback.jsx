import classNames from 'classnames/bind';

import style from './Feedback.module.scss'
const cx = classNames.bind(style);
function Feedback() {
    return ( <h1>This is page Feedback</h1> );
}

export default Feedback;